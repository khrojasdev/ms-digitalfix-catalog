# Perfil de desarrollo contra el Oracle que levanta digitalfix-infra.
spring:
  datasource:
    url: jdbc:oracle:thin:@//localhost:1521/XEPDB1
    username: DFX_CATALOG
    password: ${ORACLE_CATALOG_PASSWORD:digitalfix}
  jpa:
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    cl.duoc.digitalfix.catalog: DEBUG
